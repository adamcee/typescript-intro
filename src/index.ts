import { describeTheWeatherToday } from './weather-app/index';

const isSunny = true;
// TODO: Import forecast array and functions needed to describe the weather, use that real data.
console.log(describeTheWeatherToday(isSunny, 7, 4));